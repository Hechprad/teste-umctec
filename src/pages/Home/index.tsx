import React from 'react'

import * as s from './styles'

const Home: React.FC = () => {
  const title = 'HOME'

  return (
    <s.Section>
      <s.PageTitle>{title}</s.PageTitle>
    </s.Section>
  )
}

export default Home
